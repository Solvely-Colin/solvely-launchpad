# Deprecation Policy

- Deprecations are announced in release notes and docs.
- Every deprecation has at least two minor releases of overlap before removal.
- v1 schema remains compatible for all `v1.x` releases.
- Unknown `.citemplate.yml` keys produce warnings, not hard failures, in `v1.x`.
