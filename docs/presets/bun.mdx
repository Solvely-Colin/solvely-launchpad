# Preset: bun

Best for Bun-first projects.

## Generated workflows

- `ci.yml`
- `scheduled.yml`

## Typical command

```bash
npx solvely-launchpad init --preset bun --yes
```

## Notes

- Use explicit install/test commands in caller workflow when project layout is non-standard.
