# Adoption Dashboard

Primary KPI (90 days): external repos using `@v1` and `.citemplate.yml`.

Suggested tracking:

- installs/week of `@solvely/launchpad`
- unique repos referencing reusable workflows
- preset usage distribution
- conversion from setup workflow runs to merged PRs
